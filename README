Level Review Project

@author daigong

For teacher level up and review the level info.

database problem

# /tmp/.s.PGSQL.5432
# mkdir /var/run/postgresql
# ln -s  /tmp/.s.PGSQL.5432 /var/run/postgresql/.s.PGSQL.5432

git commit
#git add
#git commit -am 'commit message'
#git push origin master

#TODO List
1.人员需要单独选择学科组和部门，因为学科组的人可能没有部门 wait for asking
2.项目关闭，各个活动时间设置截止、删除特殊登录时间
3.清理没有用的代码,为UI设计做准备
4.活动可以设置时间


