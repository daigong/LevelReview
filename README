Level Review Project

@author daigong

For teacher level up and review the level info.

database problem

# /tmp/.s.PGSQL.5432
# mkdir /var/run/postgresql
# ln -s  /tmp/.s.PGSQL.5432 /var/run/postgresql/.s.PGSQL.5432

git commit
#git add
#git commit -am 'commit message'
#git push origin master

#TODO List
1.人员需要单独选择学科组和部门，因为学科组的人可能没有部门
2.分配参与活动的激活序列号
3.给功能添加模板加上菜单