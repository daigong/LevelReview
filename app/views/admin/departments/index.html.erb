<h1>Listing admin_departments</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Remark</th>
    <th>Subject</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

  <% @admin_departments.each do |admin_department| %>
      <tr>
        <td><%= admin_department.name %></td>
        <td><%= admin_department.remark %></td>
        <td><%= admin_department.subject %></td>
        <td><%= link_to 'Show', admin_department %></td>
        <td><%= link_to 'Edit', edit_admin_department_path(admin_department) %></td>
        <td><%= link_to 'Destroy', admin_department, confirm: 'Are you sure?', method: :delete %></td>
      </tr>
  <% end %>
</table>

<br/>

<%= link_to 'New Department', new_admin_department_path %>
